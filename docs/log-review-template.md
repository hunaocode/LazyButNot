# 提醒日志快速输出模板

1. 调度模式
- 当前是 `fixed-date window` / `recurrence`

2. 删除是否正常
- 旧闹钟是否从 `count=X` 降到 `0`

3. 铺设数量是否符合预期
- 理论值：`dateCount * offsets.count = ?`
- 实际值：`alarmUpdates count = ?`
- 结论：正常 / 偏多 / 偏少

4. 今天的提醒是否合理
- 今天普通提醒：存在 / 不存在 / 应跳过
- 今天截止监督：存在 / 不存在 / 应保留

5. 是否发现异常
- 无异常
- 或：重复铺设 / 清理不完整 / 漏铺 / 今天过滤逻辑异常

6. 最可能根因
- 简要结论
